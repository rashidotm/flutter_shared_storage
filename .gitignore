# Dart / Flutter
.dart_tool/
.packages
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Generated
*.g.dart
*.freezed.dart
*.mocks.dart

# Firebase
google-services.json
GoogleService-Info.plist
firebase_options.dart
.firebase/
firebase-debug.log

# Functions
functions/node_modules/
functions/lib/
functions/.runtimeconfig.json

# Local env files (real values). `.env.example` is committed.
.env
.env.*
!.env.example

# Coverage
coverage/

# Melos — pubspec_overrides.yaml is regenerated per-machine by
# `melos bootstrap` with platform-native paths, so committing it makes
# other OSes fail (Windows backslashes break Linux CI, etc.).
.melos_tool/
pubspec_overrides.yaml

# Claude Code (per-user local overrides — settings.json can still be committed).
.claude/settings.local.json
