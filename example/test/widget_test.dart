// `flutter create` scaffolds a counter-app smoke test that doesn't match
// this example's actual UI. Replaced with a no-op placeholder until a real
// smoke test can be wired up (which requires Firebase mocks — see Firebase
// Test SDK or fake_cloud_firestore for setup).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
